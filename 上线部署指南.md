# TCM Wisdom 上线部署指南

本项目是 **Astro 纯静态站点**，构建产物是 `dist/` 文件夹里的 68 个纯 HTML/CSS/JS 文件——任何静态托管平台都能免费部署，无需服务器、无需数据库，零成本运行。

---

## 上线前的唯一必改项：域名

`astro.config.mjs` 第 7 行的 `SITE` 目前是占位域名：

```js
const SITE = 'https://tcmwisdom.example.com';
```

它影响 canonical 标签、hreflang 互链、Open Graph 和 sitemap.xml。**确定域名后改这一行并重新构建**（可以让助手帮你改并 build）。

域名两种选择：

| 方案 | 说明 | 成本 |
|---|---|---|
| 平台免费二级域名 | 如 `tcm-wisdom.netlify.app` / `tcm-wisdom.pages.dev` | 0 元，即刻可用 |
| 自有域名子站 | 如 `tcm.manpuxiang.com`（在域名 DNS 加一条 CNAME 记录即可） | 0 元（域名本身已有） |

---

## 方案一：Netlify 拖拽部署（最简单，推荐首次上线）

全程不需要命令行，5 分钟完成。

1. **注册账号**：浏览器打开 https://app.netlify.com/signup ，邮箱注册即可
2. **登录后**，进入 Sites 页面（https://app.netlify.com/sites ）
3. **拖拽部署**：把本项目的 `dist` 文件夹直接拖进页面中间的虚线框
4. **完成**：Netlify 会自动分配一个 `https://随机名字.netlify.app` 的公网地址
5. **改个好记的名字**：Site configuration → Change site name，改成 `tcm-wisdom` 之类
6. **绑定自有域名**（可选）：Domain management → Add a domain，填 `tcm.manpuxiang.com`，然后去你的域名 DNS 管理处（域名在哪买的就去哪，如阿里云/腾讯云域名控制台）加一条记录：
   - 类型 `CNAME`，主机记录 `tcm`，记录值 `你的站点名.netlify.app`
   - 等 DNS 生效（几分钟到几小时），Netlify 自动配好免费 HTTPS 证书

**以后更新网站**：重新 `npm run build` 后，在 Netlify 站点的 Deploys 页面再把 `dist` 拖进去一次即可。

---

## 方案二：Cloudflare Pages（域名托管在 Cloudflare 时的最优选）

优点：免费无限带宽、免费 SSL、面向全球（含国内）访问质量都比较好。

1. 注册 https://dash.cloudflare.com （如果域名已托管在 Cloudflare 更方便）
2. 打开 https://dash.cloudflare.com → Workers & Pages → Create → Pages 选项卡
3. 两种方式：
   - **Direct Upload**：在网页上传 `dist` 文件夹的压缩包（或用 `wrangler pages deploy dist` 命令，助手可以帮你在本机跑）
   - **连接 Git 仓库**：把项目推到 GitHub 后连接，以后每次 push 自动重新构建部署（最省心，适合长期运营）
4. 绑定域名：Custom domains → Add → 填 `tcm.manpuxiang.com`（域名在 Cloudflare 托管时全自动，零配置）

---

## 方案三：Vercel（对 Astro 支持最好的平台）

1. 注册 https://vercel.com/signup
2. 方式 A：把项目推到 GitHub，在 Vercel 导入仓库，自动识别 Astro，零配置
3. 方式 B：本机装 CLI：`npm i -g vercel`，然后在项目目录运行 `vercel`，按提示登录部署
4. 免费二级域名为 `xxx.vercel.app`

> ⚠️ 注意：`*.vercel.app` 在中国大陆访问经常不稳定。本站目标读者在海外，不影响；但你自己在国内验收时打不开属正常现象，用 Netlify 或 Cloudflare Pages 则国内外都稳定。

---

## 平台对比速查

| | Netlify 拖拽 | Cloudflare Pages | Vercel |
|---|---|---|---|
| 是否需要命令行 | ❌ 不需要 | Direct Upload 可免 | 需要 CLI 或 Git |
| 免费带宽 | 100GB/月 | 无限制 | 100GB/月 |
| 国内访问 | 较稳定 | 稳定 | vercel.app 不稳定 |
| 绑定自有域名 | ✅ 免费 | ✅ 免费 | ✅ 免费 |
| 适合 | 首次上线、最简单 | 长期运营、最优 | 开发者习惯 |

---

## 上线后收尾（SEO）

1. **Google Search Console**：https://search.google.com/search-console 添加资源 → 验证域名 → 提交 `https://你的域名/sitemap.xml`
2. **Bing Webmaster Tools**（可选）：https://www.bing.com/webmasters 同样提交 sitemap
3. 等搜索引擎收录（通常几天到几周）

---

## 给助手的交接说明

- 域名确定后：改 `astro.config.mjs` 的 `SITE` → `npx astro build` → 重新部署 `dist`
- 本地预览：`npx astro preview --host 0.0.0.0 --port 4321`（手机同 Wi-Fi 可访问）
